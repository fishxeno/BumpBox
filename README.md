# React + TypeScript + Vite

to start the server
npm install
npm start
this will run node server.js

tp start webapp
npm install
npm start
this will run npm run dev

to deploy to aws,
final commit message needs to include [deploy]

don't share aws link with anyone
